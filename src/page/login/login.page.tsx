import React, {FC} from "react";

type LoginProps = {
    params: any
}

export default function Login () {

    return (
        <section className={`dashboard`}>
            Login
        </section>
    )
}